---
title: "Setting Up Next.js"
sidebarTitle: "Next.js Setup"
description: "Learn how to set up a Next.js project for your headless WooCommerce store"
---

# Introduction

[Next.js](https://nextjs.org) is a powerful React framework that enables you to build fast, production-ready web applications. It's an excellent choice for headless storefronts with CoCart because of its server-side rendering, API routes, excellent performance, and built-in optimizations.

This guide will walk you through setting up a Next.js project configured to work with CoCart API.

## Why Next.js for Headless Commerce?

- **Hybrid rendering** - Use SSR, SSG, or ISR based on your needs
- **Built-in API routes** - Create backend endpoints without a separate server
- **Optimized performance** - Automatic code splitting and image optimization
- **SEO friendly** - Server-side rendering for better search engine visibility
- **Great DX** - Fast refresh, TypeScript support, and comprehensive documentation
- **Vercel deployment** - Zero-config deployment with built-in CI/CD

## Prerequisites

- Node.js 18.17 or higher
- A WordPress site with WooCommerce installed
- CoCart plugin installed and activated
- Basic knowledge of React and JavaScript

## Creating a New Next.js Project

Create a new Next.js project using the official CLI:

```bash
npx create-next-app@latest my-headless-store
```

When prompted, choose the following options:

- **Would you like to use TypeScript?** → Yes (recommended) or No
- **Would you like to use ESLint?** → Yes (recommended)
- **Would you like to use Tailwind CSS?** → Yes (recommended)
- **Would you like your code inside a `src/` directory?** → Yes (recommended)
- **Would you like to use App Router?** → Yes (recommended)
- **Would you like to use Turbopack for `next dev`?** → Yes (optional, for faster dev)
- **Would you like to customize the import alias?** → No (unless you have preference)

Navigate to your project:

```bash
cd my-headless-store
```

Install the CoCart TypeScript SDK:

```bash
npm install @cocartheadless/sdk
```

<Tip>
  The SDK ships a dedicated Next.js adapter (`@cocartheadless/sdk/nextjs`) with `createBrowserClient()` and `createServerClient()` helpers. It handles the split between Client and Server Components for you — no cookies required. See the [Next.js adapter guide](/sdk/typescript/frameworks/nextjs) for the full API.
</Tip>

## Project Structure

Your Next.js project will have this structure:

```
my-headless-store/
├── src/
│   ├── app/              # App Router pages and layouts
│   │   ├── layout.tsx    # Root layout
│   │   ├── page.tsx      # Home page
│   │   └── api/          # API routes
│   ├── components/       # Reusable React components
│   ├── lib/              # Utility functions and API clients
│   └── styles/           # Global styles
├── public/               # Static assets
├── next.config.js        # Next.js configuration
├── tailwind.config.ts    # Tailwind configuration
├── package.json
└── tsconfig.json         # TypeScript config (if using TS)
```

Create the necessary folders:

```bash
mkdir -p src/components src/lib
```

## Environment Configuration

Create a `.env.local` file in your project root:

```bash
COCART_STORE_URL=https://yourstore.com
NEXT_PUBLIC_COCART_STORE_URL=https://yourstore.com
```

- `COCART_STORE_URL` is read by Server Components and Route Handlers.
- `NEXT_PUBLIC_COCART_STORE_URL` is the same value exposed to the browser client.

<Note>
  Variables prefixed with `NEXT_PUBLIC_` are exposed to the browser. Keep the server-only variable name without that prefix for anything that shouldn't reach the client bundle.
</Note>

Add `.env.local` to your `.gitignore` (it should already be there by default):

```bash
# .gitignore already includes
.env*.local
```

Create a `.env.example` for your team:

```bash
# .env.example
COCART_STORE_URL=https://yourstore.com
NEXT_PUBLIC_COCART_STORE_URL=https://yourstore.com
```

## Creating the CoCart API Client

Use the SDK's built-in Next.js adapter, which handles the split between Server Components (no cookies, reads the cart key from request headers) and Client Components (persists the cart key in `localStorage`).

Create `src/lib/cocart.ts`:

```typescript
'use client';

import { createBrowserClient, attachCartKeyHeader } from '@cocartheadless/sdk/nextjs';

let browserClient: ReturnType<typeof createBrowserClient> | null = null;

// Browser client (used in Client Components, e.g. "Add to Cart" buttons)
export function getClient() {
  if (!browserClient) {
    browserClient = createBrowserClient(process.env.NEXT_PUBLIC_COCART_STORE_URL!);
    attachCartKeyHeader(browserClient);
  }
  return browserClient;
}
```

<Tip>
  The SDK handles cart key persistence, authentication, and request formatting automatically. See the [Next.js adapter guide](/sdk/typescript/frameworks/nextjs) for advanced configuration, including encrypted storage, middleware-based cart key forwarding, and login/session merging.
</Tip>

## Creating API Routes

Next.js API routes (Route Handlers) allow you to create backend endpoints. Create one for adding items to the cart, using `createServerClient()` to read the cart key straight from the incoming request headers:

Create `src/app/api/cart/add/route.ts`:

```typescript
import { NextRequest, NextResponse } from 'next/server';
import { createServerClient } from '@cocartheadless/sdk/nextjs';

export async function POST(request: NextRequest) {
  const client = createServerClient(
    process.env.COCART_STORE_URL!,
    request.headers,
  );

  try {
    const { productId, quantity = 1 } = await request.json();
    const response = await client.cart().addItem(productId, quantity);

    return NextResponse.json(response.toObject(), { status: 200 });
  } catch (error) {
    return NextResponse.json(
      { error: (error as Error).message },
      { status: 500 }
    );
  }
}
```

## Updating the Root Layout

Update your root layout at `src/app/layout.tsx`:

```typescript
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "My Headless Store",
  description: "Your headless WooCommerce store powered by CoCart",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}
```

## Testing Your Setup

Update the homepage at `src/app/page.tsx`. Products are public, so this uses the plain `CoCart` client directly rather than the Next.js adapter:

```typescript
import { CoCart } from '@cocartheadless/sdk';

export default async function Home() {
  const client = new CoCart(process.env.COCART_STORE_URL!);
  const response = await client.products().all({ per_page: 3 });
  const products = response.toObject() as any[];

  return (
    <main className="container mx-auto px-4 py-12">
      <h1 className="text-3xl font-bold text-zinc-900 dark:text-white mb-8">
        Welcome to Your Headless Store
      </h1>

      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
        {products.map((product) => (
          <div key={product.id} className="border rounded-lg p-4">
            <h2 className="font-semibold">{product.name}</h2>
            <p className="text-zinc-600">
              {product.prices.currency.currency_symbol}{product.prices.price}
            </p>
          </div>
        ))}
      </div>
    </main>
  );
}
```

<Note>
  CoCart returns prices as smallest-unit integers (e.g. `4599` for $45.99) alongside a `currency` object describing how to format them. The [TypeScript SDK utilities guide](/sdk/typescript/utilities) covers `CurrencyFormatter` for formatting these correctly.
</Note>

## Running Your Project

Start the development server:

```bash
npm run dev
```

Visit `http://localhost:3000` to see your store.

## Building for Production

Build your site for production:

```bash
npm run build
```

Start the production server locally:

```bash
npm run start
```

## Caching Strategies

Next.js offers several caching strategies for optimal performance:

### Static Generation (SSG)
For pages that can be pre-rendered at build time:

```typescript
import { CoCart } from '@cocartheadless/sdk';

export const revalidate = 3600; // Revalidate every hour

export default async function ProductsPage() {
  const client = new CoCart(process.env.COCART_STORE_URL!);
  const response = await client.products().all({ per_page: 12 });
  const products = response.toObject() as any[];
  // ...
}
```

### Incremental Static Regeneration (ISR)
For pages that need periodic updates:

```typescript
import { CoCart } from '@cocartheadless/sdk';

export async function generateStaticParams() {
  const client = new CoCart(process.env.COCART_STORE_URL!);
  const response = await client.products().all({ per_page: 100 });
  const products = response.toObject() as any[];
  return products.map((product) => ({ id: String(product.id) }));
}

export default async function ProductPage({ params }: { params: { id: string } }) {
  const client = new CoCart(process.env.COCART_STORE_URL!);
  const response = await client.products().find(params.id);
  const product = response.toObject();
  // ...
}
```

### Server-Side Rendering (SSR)
For dynamic pages that need fresh data, such as the cart, use `createServerClient()` so the cart key is read from the incoming request headers:

```typescript
import { headers } from 'next/headers';
import { createServerClient } from '@cocartheadless/sdk/nextjs';

export const dynamic = 'force-dynamic';

export default async function CartPage() {
  const headersList = await headers();
  const client = createServerClient(process.env.COCART_STORE_URL!, headersList);
  const cart = await client.cart().get();
  // ...
}
```

## Deployment Options

Next.js sites can be deployed to various platforms:

- **Vercel** - Zero configuration deployment (recommended)
- **Netlify** - Easy deployment with built-in features
- **AWS Amplify** - Full-stack deployment
- **Docker** - Containerized deployment
- **Your own server** - Node.js server required

### Deploying to Vercel

1. Push your code to GitHub, GitLab, or Bitbucket
2. Import your project on [Vercel](https://vercel.com)
3. Add your environment variables
4. Deploy!

## Next Steps

Now that your Next.js project is set up with CoCart:

1. Read the [Next.js adapter guide](/sdk/typescript/frameworks/nextjs) for cart persistence, middleware, and guest-to-customer login merging
2. Explore the [TypeScript SDK](/sdk/typescript) for full cart management, authentication, and more
3. Implement a checkout flow
4. Add user authentication with JWT
5. Optimize images with Next.js Image component
6. Add loading states and error boundaries

## Troubleshooting

### CORS Errors

If you encounter CORS errors, you may need to configure WordPress to allow cross-origin requests. See [CORS documentation](/cors).

### API Connection Issues

1. Verify `COCART_STORE_URL` and `NEXT_PUBLIC_COCART_STORE_URL` are correct in `.env.local`
2. Ensure CoCart is installed and activated
3. Check that WooCommerce is configured properly
4. Test API endpoints directly in your browser or Postman

### Hydration Errors

If you see hydration mismatches:
- Ensure you're not using browser-only APIs during SSR
- Use `'use client'` directive for client-only components
- Check that your data is consistent between server and client renders

## Resources

- [CoCart TypeScript SDK](/sdk/typescript) — Full SDK documentation
- [Next.js Adapter Guide](/sdk/typescript/frameworks/nextjs) — Advanced Next.js integration
- [Next.js Documentation](https://nextjs.org/docs)
- [CoCart API Reference](../../api-reference)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [React Documentation](https://react.dev)
