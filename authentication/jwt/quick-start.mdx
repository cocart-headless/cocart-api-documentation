---
title: "Quick Start"
sidebarTitle: "Quick Start"
description: "Get started with CoCart JWT Authentication — log in, use tokens, refresh, and validate in minutes."
'og:title': "Quick Start Guide for CoCart JWT Authentication"
'og:description': "Quick start guide to using CoCart JWT Authentication."
---

This guide assumes you have already installed and configured CoCart JWT Authentication. If not,  please follow the [setup guide](/authentication/jwt) first.

<Tip>
    In the following request examples below, you would replace `<username>`, `<password>`, `<JWT-TOKEN>` and `<REFRESH-TOKEN>` with your credentials before sending the request.
</Tip>

## Authentication Flow

### 1. Get a Token

To authenticate a user and get a JWT token:

<CodeGroup>
    ```bash cURL
    curl -X POST \
        https://your-store.com/wp-json/cocart/v2/login \
        -H "Content-Type: application/json" \
        -d '{"username": "<username>", "password": "<password>"}'
    ```

    ```php PHP
    <?php
    $ch = curl_init();
    
    curl_setopt_array($ch, [
        CURLOPT_URL => 'https://your-store.com/wp-json/cocart/v2/login',
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_CUSTOMREQUEST => "POST",
        CURLOPT_HTTPHEADER => [
            'Accept: application/json',
            'Authorization: Basic ' . base64_encode(<username> . ':' . <password>)
        ]
    ]);
    
    $response = curl_exec($ch);
    $http_code = curl_getinfo($ch, CURLINFO_HTTP_CODE);
    
    curl_close($ch);
    ```

    ```javascript JavaScript
    const auth = btoa('<username>:<password>');
    fetch('https://your-store.com/wp-json/cocart/v2/login', {
        method: 'POST',
        headers: {
            'Accept': 'application/json',
            'Authorization': `Basic ${auth}`
        }
    });
    ```
</CodeGroup>

<Info>
  Using an SDK? The [TypeScript](/sdk/typescript), [PHP](/sdk/php), [Python](/sdk/python), and [Go](/sdk/go) SDKs handle token storage and renewal automatically.
</Info>

<Tabs>
  <Tab title="TypeScript">
    ```typescript
    import { CoCart } from '@cocartheadless/sdk';

    const client = new CoCart('https://your-store.com');
    await client.login('<username>', '<password>');

    // JWT token is stored automatically — all subsequent requests use it
    const cart = await client.cart().get();
    ```
  </Tab>
  <Tab title="PHP">
    ```php
    use CoCart\CoCart;

    $client = new CoCart('https://your-store.com');
    $client->login('<username>', '<password>');

    // JWT token is stored automatically — all subsequent requests use it
    $cart = $client->cart()->get();
    ```
  </Tab>
  <Tab title="Python">
    ```python
    from cocart import CoCart

    client = CoCart('https://your-store.com')
    client.login('<username>', '<password>')

    # JWT token is stored automatically — all subsequent requests use it
    cart = client.cart().get()
    ```
  </Tab>
  <Tab title="Go">
    ```go
    import cocart "github.com/cocart-headless/cocart-sdk-go"

    client := cocart.NewClient("https://your-store.com")
    ctx := context.Background()

    if err := client.JWT().Login(ctx, "<username>", "<password>"); err != nil {
        log.Fatal(err)
    }

    // JWT token is stored automatically — all subsequent requests use it
    cart, err := client.Cart().Get(ctx, nil)
    ```
  </Tab>
</Tabs>

```json
{
    "user_id": "123",
    "first_name": "John",
    "last_name": "Smith",
    "display_name": "john",
    "role": "Customer",
    "avatar_urls": {},
    "email": "users@emailaddress.xyz",
    "extras": {
        "jwt_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..aBuyRwAtvGb6SI4BB_MN4NYN01jqVZN4PPnd1jfW2UA",
        "jwt_refresh": "90efc95f1d85e465951d10c309897629524b7fc1b40dfab75ed68f7c8540468a05b8b26995685821f52cf736edb566f3317432288af4c6e4edc281f6ab7af371"
    },
    "dev_note": "Don't forget to store the users login information in order to authenticate all other routes with CoCart."
}
```

<Info>
    If you require to pass the user-agent header when making requests. Applying it when logging in must be done for the token to remain valid. If you decide to pass the user-agent header after, then the token will no longer be valid and you will have to request a new one.
</Info>

### 2. Use the Token

Make authenticated requests using the token. Here’s an example using Cart endpoint to get the current user’s cart:

<CodeGroup>
    ```bash cURL
    curl -X GET 'https://your-store.com/wp-json/cocart/v2/cart' \
    -H 'Authorization: Bearer <JWT-TOKEN>'
    ```

    ```php PHP
    <?php
    $token = '<JWT-TOKEN>';
    $ch = curl_init();
    curl_setopt_array($ch, [
        CURLOPT_URL => 'https://your-store.com/wp-json/cocart/v2/cart',
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_HTTPHEADER => [
            'Authorization: Bearer ' . $token
        ]
    ]);

    $cart = curl_exec($ch);
    curl_close($ch);
    ```

    ```javascript JavaScript
    const token = await getToken();
    const cart = await fetch('https://your-store.com/wp-json/cocart/v2/cart', {
        headers: {
            'Authorization': `Bearer ${token}`
        }
    });
    ```
</CodeGroup>

<Tabs>
  <Tab title="TypeScript">
    ```typescript
    // After login(), the SDK sends the JWT token automatically on every request
    const cart = await client.cart().get();
    ```
  </Tab>
  <Tab title="PHP">
    ```php
    // After login(), the SDK sends the JWT token automatically on every request
    $cart = $client->cart()->get();
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # After login(), the SDK sends the JWT token automatically on every request
    cart = client.cart().get()
    ```
  </Tab>
  <Tab title="Go">
    ```go
    // After JWT().Login(), the SDK sends the JWT token automatically on every request
    cart, err := client.Cart().Get(ctx, nil)
    ```
  </Tab>
</Tabs>

### 3. Refresh Token

When the access token expires, use the refresh token to get a new one:

<CodeGroup>
    ```bash cURL
    curl -X POST \
        https://your-site.com/wp-json/cocart/jwt/refresh-token \
        -H "Content-Type: application/json" \
        -d '{"refresh_token": "<REFRESH-TOKEN>"}'
    ```

    ```php PHP
    <?php
    $token = '<REFRESH-TOKEN>';
    $ch = curl_init();
    curl_setopt_array($ch, [
        CURLOPT_URL => 'https://your-store.com/wp-json/cocart/jwt/refresh-token',
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_CUSTOMREQUEST => "POST",
        CURLOPT_HTTPHEADER => [
            'Authorization: Bearer ' . $token
        ]
    ]);

    $cart = curl_exec($ch);
    curl_close($ch);
    ```

    ```javascript JavaScript
    const token = await refreshToken();
    const cart = await fetch('https://your-store.com/wp-json/cocart/jwt/refresh-token', {
        method: 'POST',
        headers: {
            'Authorization': `Bearer ${token}`
        }
    });
    ```
</CodeGroup>

```json
{
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..zHEHjVLE0Rrr7yY4z51bjhnm5ndkbR6J1nDzJNOZTK0",
    "refresh_token": "7dfc00d346277468b975a22768f861702b056e20f7cd84675b4dd4c0eb1148f034ae2610c548458a55213d62ea6034006466919166841e5f6797caeac5bd5e27"
}
```

<Tabs>
  <Tab title="TypeScript">
    ```typescript
    // Refresh token is stored from login() — call refresh() when the access token expires
    await client.jwt().refresh();
    ```
  </Tab>
  <Tab title="PHP">
    ```php
    // Refresh token is stored from login() — call refresh() when the access token expires
    $client->jwt()->refresh();
    ```
  </Tab>
  <Tab title="Python">
    ```python
    # Refresh token is stored from login() — call refresh() when the access token expires
    client.jwt().refresh()
    ```
  </Tab>
  <Tab title="Go">
    ```go
    // Refresh token is stored from JWT().Login() — call Refresh() when the access token expires
    if err := client.JWT().Refresh(ctx); err != nil {
        log.Fatal(err)
    }
    ```
  </Tab>
</Tabs>

<Warning>
    Remember to never expose your JWT secret key or store tokens in plain text. Always use secure storage methods appropriate for your platform.
</Warning>

### 4. Validate Token

To keep check of a valid token:

```bash cURL
curl -X POST \
    https://your-site.com/wp-json/cocart/jwt/validate-token \
    -H "Authorization: Bearer YOUR-JWT-TOKEN"
```

**Successful**

```json
{
    "message": "Token is valid."
}
```

**Invalid Token**

```json
{
    "code": "cocart_authentication_error",
    "message": "Authentication failed.",
    "data": {
        "status": 401
    }
}
```
